package org.donkey.app;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeClusterExtenderAppIT extends ClusterExtenderAppTest {

    // Execute the same tests but in native mode.
}